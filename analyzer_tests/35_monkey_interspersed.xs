// Monkey test: characters interspersed in valid tokens
fu@nc ma#in() {
    va$r x% = 5^;
    va&r y* = 1(0);

    pr!intf("te@st\n#");

    var z = x +++ y --- z *** w;
    ############

    if if if if (x) {
        retu@rn retu#rn;
    }

    wh!ile wh@ile (true false maybe) {
        br@@eak bre##ak;
    }
}

st@ruct Po!int im#plements op$s.Disp%lay {
    x@: i!nt = 0#;
    y$: i%nt = 0^;

    fu&nc di*splay() st(ring {
        ret)urn "te_st";
    }
}

fu+nc te=st<T[: Sh]ow>{(}t): T>) T? {
    ret?urn t;
}

var glob###al = Sha@@red<int>{{{{42}}}};;;
