// Ultimate chaos monkey test
{{{[[[(((<<<<>>>>))))]]]}}}}

func main((((( {
    ////****\\\\****////
    var s = string.format("""""""
    var x = {{{{{}}}}};

    while while while {
        for for for {
            if if if {
                switch switch switch {
                    }}}}{{{{
                }
            }
        }
    }

    @@@@@@@@@@@@@@@@@@@@
    var arr = Array<Array<Array<Array<Array<
    $$$$$$$$$$$$$$$$$$$$

    func func func func {
        struct struct struct {
            enum enum enum {
                interface interface interface {
                    ^^^^^^^^^^^^
                }
            }
        }
    }
}

////////********\\\\\\\\********////////

struct {{{{Point}}}} {
    x: int: int: int: int;
    y y y y: string string string;

    func ((((bar)))) {
        this this this this.
        ......................
    }
}

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
var var var = func func func;
