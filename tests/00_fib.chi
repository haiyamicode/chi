func main() {
    int N;
    N = 10; // Should return 55
    let value = fib(N);
    printf("fib: {}\n", value);
}

func fib(int n) int {
    if (n < 2) {
        return n;
    }
    return (fib(n - 2) + fib(n - 1));
}