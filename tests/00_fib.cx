int main() {
    int N;
    N = 10; // Should return 55
    let value = fib(N);
    printf("fib: {}\n", value);
    return 0;
}

int fib(int n) {
    if (n < 2) {
        return n;
    }
    return (fib(n - 2) + fib(n - 1));
}